# Hospital Report Transfer Validator

## Run in VS Code
1. Open this folder in VS Code.
2. Open Terminal.
3. Run: `pip install -r requirements.txt`
4. Run: `python app.py`
5. Open `http://127.0.0.1:5000`

Use the same report in both boxes for ACCEPTED. Change one value in the received report for REJECTED.
